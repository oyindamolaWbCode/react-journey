const TestComponent = () => {
    return ( 
        <div className="Reuse">
        <p>Another day with Reactjs using reuse component</p>
        <p>Well it only works with version 14</p>
        </div>
     );
}
 
export default TestComponent;